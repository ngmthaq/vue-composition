// import type { UsePromiseCallback, UsePromiseStatus } from "@/types/hooks";
// import type { Ref } from "vue";
// import { onBeforeMount, ref } from "vue";

// export function usePromise<M>(callback: UsePromiseCallback<M>) {
//   const status: Ref<UsePromiseStatus> = ref("idle");

//   onBeforeMount(() => {});

//   return [status];
// }
